import { StyleSheet } from "react-native"

export default styles = StyleSheet.create({
    inputField: {
        borderRadius: 2,
        fontSize: 20,
        borderRadius: 2,
        borderColor: 'black',
        borderWidth: 1,
        padding: 5,
        margin: 10,
    }  
})